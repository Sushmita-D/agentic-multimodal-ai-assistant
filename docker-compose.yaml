version: "3.9"

services:

  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile

    container_name: agentic-backend

    ports:
      - "8000:8000"

    env_file:
      - ./Backend/.env

    volumes:
      - ./Backend:/app

    restart: unless-stopped